class Solution:
    def countKDifference(self, nums: List[int], k: int) -> int:
        ans = 0
        freq = {}
        # for i in range(len(nums)):
        #     for j in range(i+1,len(nums)):
        #         if abs(nums[i]-nums[j]) == k:
        #             ans+=1

        for num in nums:

            ans+= freq.get(num - k,0)
            ans+= freq.get(num + k,0)

            freq[num] = freq.get(num,0) + 1
        return ans
