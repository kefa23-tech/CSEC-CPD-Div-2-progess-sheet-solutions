class Solution:
    def missingNumber(self, nums: List[int]) -> int:

        # m = 0
        # while m in nums:
        #     m+=1
        # return m

        n = len(nums)

        expected = n*(n+1)//2

        has = sum(nums)

        missing = expected - has

        return missing
